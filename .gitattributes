# Normalise line endings so CI shell scripts and YAML never get CRLF,
# regardless of a contributor's core.autocrlf setting.
* text=auto eol=lf

# Lockfile is generated; collapse diffs in review.
pnpm-lock.yaml linguist-generated=true -diff
