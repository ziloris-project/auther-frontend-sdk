---
"@auther-sdk/frontend": patch
---

The default backend URL is now a single DEFAULT_ENDPOINT constant instead of being hardcoded in two places, and its casing is normalized to match the other SDKs. No change to the resolved host.
